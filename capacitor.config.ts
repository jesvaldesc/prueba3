import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jesus.proyectoSemestral',
  appName: 'Proyecto Semestral',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
